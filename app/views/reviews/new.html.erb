<div class="container">
  <h1>Write a review</h1>

  <%= simple_form_for([@restaurant, @review]) do |r| %>
  <%=  r.input :content %>
  <%=  r.input :rating %>
  <%=  r.submit "Submit review", class: "btn btn-primary" %>
  <% end %>

</div>
